package plugins

import (
	"errors"
	"github.com/just1689/jstooling/model"
)

var Ytt model.StandardFunction = func(in []string) (msg string, err error) {
	err = errors.New("not implemented")
	return
}
